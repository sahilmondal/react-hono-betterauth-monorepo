// Example code for .service.ts

// export async function getUserById(userId: string) {
//   const user = await db.select().from(users).where(eq(users.id, userId)).limit(1)

//   if (!user.length) {
//     throw new NotFoundError('User')
//   }
// ...
// }
